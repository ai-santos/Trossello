import { expect } from './setup'
import { Router } from 'SimpleReactRouter'

describe('SimpleReactRouter', () => {

  describe('Router', () => {
    it('should route', () => {
      expect(Router).to.be.a.func
    })
  })

})
